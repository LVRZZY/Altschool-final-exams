apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: voting-app
spec:
  ingressClassName: external-nginx
  rules:
  - host: archibongdonald.me
    http:
      paths:
      - path: /
        pathType: prefix
        backend:
          service:
            name: test
            port:
              number: 80